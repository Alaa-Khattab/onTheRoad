import React, { Component } from 'react';

const SideBar = () => {
  return(
    <aside className="sidebar">
      <button type="button">Find a ride</button>
      <button type="button">My rides</button>
      <button type="button">New ride</button>
    </aside>
  );
}

export default SideBar;
